# Base dependencies
python-dotenv==1.0.1
requests==2.31.0
aiohttp==3.9.3
certifi==2024.2.2
solders==0.19.0
base58==2.1.1

# Note: httpx and python-telegram-bot are installed separately 
# by the installation scripts to handle version conflicts.
# solana is installed directly from source. 