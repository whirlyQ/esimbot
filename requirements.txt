# Main requirements without version conflicts
python-telegram-bot==20.8
python-dotenv==1.0.1
requests==2.31.0
aiohttp==3.9.3
certifi==2024.2.2
solders==0.19.0
base58==2.1.1

# Note: httpx and solana packages are installed separately by install_deps.sh
# to avoid dependency conflicts
# solana==0.31.0 